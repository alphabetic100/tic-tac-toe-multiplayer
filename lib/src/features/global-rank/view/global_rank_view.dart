import 'package:flutter/material.dart';

class GlobalRankView extends StatelessWidget {
  const GlobalRankView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
